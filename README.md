# Vite + Flask

To setup on python:
```
python -m venv venv

(windows)
.\venv\Scripts\activate.bat

(linux)
source venv/bin/activate

pip install -r requirements.txt
```
Python requirements:
- flask
- flask-cors
- openai
- matplotlib

React Modules Used:
- idk yet